# NOTE ON VERSIONS: pinned to the earliest patch releases confirmed to ship
# prebuilt Python 3.14 (cp314) wheels, so `pip install` never falls back to
# compiling from source (which requires build toolchains most machines
# don't have, e.g. Meson/Visual Studio for pandas/numpy on Windows).
# If you're on Python 3.12/3.13 instead, these versions work fine too.

# --- Web framework ---
fastapi==0.118.0
uvicorn[standard]==0.38.0

# --- Database / ORM ---
sqlalchemy==2.0.45
psycopg2-binary==2.9.12
alembic==1.14.1

# --- Settings / validation ---
pydantic==2.12.4
pydantic-settings==2.14.2

# --- HTTP client (FPL API) ---
httpx==0.28.1
tenacity==9.0.0

# --- Data / ML ---
pandas==2.3.3
numpy==2.3.5
scikit-learn==1.7.2
joblib==1.4.2

# --- Optimization ---
ortools==9.15.6755

# --- Scheduling ---
apscheduler==3.11.0

# --- Frontend (Phase 8) ---
jinja2==3.1.5
python-multipart==0.0.20
streamlit==1.50.0
plotly==5.24.1

# --- Phase 12: historical pretraining ---
rapidfuzz==3.14.5

# --- Utilities ---
python-dotenv==1.0.1
loguru==0.7.3

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
respx>=0.22.0

## ----Phase 3 ----
httpx>=0.27
tenacity>=8.2
pydantic-settings>=2.0
pytest-asyncio>=0.23 # test-only, if not already present
